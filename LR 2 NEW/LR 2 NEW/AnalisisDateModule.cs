

using System.Collections.Generic;

namespace LR_2_NEW
{
    class AnalisisDateModule
    {
        static public void СataloguesThisНear(Disk diskС, Disk diskD, string user, List<catalog> C, List<catalog> D) // сортировка каталогов этого года
        {
            List<catalog> Yearthese = new List<catalog>();
            if (diskС.Name == user)
            {
                if (diskС.catalogs.Contains(2025))
                {

                }
            }
        }
    }
}
